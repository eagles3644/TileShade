<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/board"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content">

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/game_buttons"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">

        <Button
            android:id="@+id/game_pause"
            android:layout_width="@dimen/mode_button_width"
            android:layout_height="@dimen/mode_button_height"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="@dimen/mode_button_margin_bottom"
            android:text="@string/pause"
            android:textStyle="bold"
            android:textSize="@dimen/mode_button_text_size"/>

        <Button
            android:id="@+id/game_reset"
            android:layout_width="@dimen/mode_button_width"
            android:layout_height="@dimen/mode_button_height"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/game_pause"
            android:layout_marginBottom="@dimen/mode_button_margin_bottom"
            android:text="@string/start_over"
            android:textStyle="bold"
            android:textSize="@dimen/mode_button_text_size"/>

    </RelativeLayout>
    
</RelativeLayout>